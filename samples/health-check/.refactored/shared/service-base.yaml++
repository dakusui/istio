apiVersion: v1
kind: Service
metadata:
  labels:
    app: "eval:string:refexpr(\".metadata.name\")"
    service: "eval:string:refexpr(\".metadata.name\")"
spec:
  selector:
    app: "eval:string:refexpr(\".metadata.name\")"
