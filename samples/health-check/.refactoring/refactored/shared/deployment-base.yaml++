apiVersion: apps/v1
kind: Deployment
metadata:
  name: "eval:string:refexpr(\"._app\")"
spec:
  selector:
    matchLabels:
      app: "eval:string:refexpr(\"._app\")"
  template:
    metadata:
      labels:
        app: "eval:string:refexpr(\"._app\")"
    spec:
      containers: []
