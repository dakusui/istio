$extends:
  - httproute-reviews-base.yaml++
spec:
  rules:
    - backendRefs:
        - "eval:object:httproute::backendRef(\"v1\"; 9080; 50)"
        - "eval:object:httproute::backendRef(\"v3\"; 9080; 50)"
