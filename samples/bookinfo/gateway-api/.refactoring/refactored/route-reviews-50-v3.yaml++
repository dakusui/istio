$extends:
  - httproute-reviews-base.yaml++
  - funcs.jq
spec:
  rules:
    - backendRefs:
        - "eval:object:funcs::backendRef(\"v1\"; 9080; 50)"
        - "eval:object:funcs::backendRef(\"v3\"; 9080; 50)"
