$extends:
  - httproute.jq
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "eval:string:refexpr(\"._svc\")"
spec:
  parentRefs:
    - "eval:object:httproute::parentRef(9080)"
  rules:
    - backendRefs:
        - "eval:object:httproute::backendRef(\"v1\"; 9080)"
