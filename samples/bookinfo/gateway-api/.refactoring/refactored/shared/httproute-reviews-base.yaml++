$extends:
  - funcs.jq
_svc: reviews
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: reviews
spec:
  parentRefs:
    - "eval:object:funcs::parentRef(reftag(\"_svc\"); 9080)"
