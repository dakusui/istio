apiVersion: v1
kind: Service
metadata:
  name: "eval:string:refexpr(\"._app\")"
  labels:
    app: "eval:string:refexpr(\"._app\")"
    service: "eval:string:refexpr(\"._app\")"
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: "eval:string:refexpr(\"._app\")"
