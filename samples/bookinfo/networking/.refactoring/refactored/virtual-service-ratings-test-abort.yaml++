$extends:
  - shared/virtual-service-base.yaml++
  - spec.jq
_svc: ratings
spec:
  http:
  - match:
    - headers:
        end-user:
          exact: jason
    fault:
      abort:
        percentage:
          value: 100.0
        httpStatus: 500
    route:
    - destination: "eval:object:spec::routing_destination(\"v1\")"
  - route:
    - destination: "eval:object:spec::routing_destination(\"v1\")"
