$extends:
  - shared/virtual-service-base.yaml++
  - spec.jq
_svc: reviews
spec:
  http:
  - route:
    - destination: "eval:object:spec::routing_destination(\"v1\")"
      weight: 50
    - destination: "eval:object:spec::routing_destination(\"v3\")"
      weight: 50
