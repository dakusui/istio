apiVersion: v1
kind: Service
metadata:
  labels:
    app: mtls-echo
  name: mtls-echo
spec:
  selector:
    app: mtls-echo
  type: ClusterIP
  ports:
  - name: https
    protocol: TCP
    port: 8443
    targetPort: 8443
