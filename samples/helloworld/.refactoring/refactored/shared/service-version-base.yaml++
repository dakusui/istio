apiVersion: v1
kind: Service
metadata:
  name: "eval:string:\"helloworld-\" + refexpr(\"._version\")"
spec:
  ports:
    - port: 5000
      name: http
  selector:
    app: helloworld
    version: "eval:string:refexpr(\"._version\")"
